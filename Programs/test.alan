main () : proc
    rings : int;
    ns : int[15];
    -- var : int;
    -- fingers : byte ;
    d : byte;

    compute (x : reference int) : proc
       var : int;
       {
           -- return x[0];
           -- x[7] = 3;
           x = readInteger();
           return;
       }

{
    -- Main
    compute(rings);
    writeInteger(rings);
    return;
}
