main () : proc
    x : int;
    z : int ;
    -- ns : int[15];
    -- str : byte[100];
    -- b : byte;

    compute (y : int) : proc
       -- default val for y is 0
       {
       writeString("\n x from inside compute is: ");
       writeInteger(x);
       writeString("\n y from inside compute is: ");
       writeInteger(y);
       y = y + 1;
       x = x + 1;

       }


{
    -- Main
    x = 1;

    while ( x < 10 ){
    compute(x);
    }
    writeString("\n X after the loop is: ");
    writeInteger(x);
    return;
}
