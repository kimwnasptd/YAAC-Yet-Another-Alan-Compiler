main () : proc
    x : int;
    ns : int[15];
    str : byte[100];
    b : byte;

    compute (x : reference int) : proc
       var : int;
       {
           -- return x[0];
           -- x[7] = 3;
           -- x = readInteger();
           return;
       }

{
    -- Main
    -- compute(rings);
    -- writeInteger(rings);
    -- readString(5, str);
    -- d = readChar();
    x = 257;
    -- b = shrink(x);
    b = readByte();
    writeString("Byte is: ");
    -- writeInteger(extend(b));
    writeByte(b);
    return;
}
