main () : proc
    rings : int;
    ns : int[5];
    var : int;

    compute (x : int) : int
        var : int;
        {
            return x + x * 2;
        }
{
    -- rings = 2;
    -- compute(5);
    -- rings = rings * rings + 3;
    -- return rings * var;
    return;
}
