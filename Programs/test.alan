main () : proc
    rings : int;
    ns : int[15];
    -- var : int;
    -- fingers : byte ;

    compute (x : reference int[]) : int
       var : int;
       {
           -- return x[0];
           x[7] = 3;
           return 6;
       }

{
    -- rings = compute(ns);
    ns[5] = 12;
    -- fingers = '5' ;
    -- rings = - rings ;
    -- writeInteger(2);
    -- rings = compute(5);
    -- rings = compute(rings);
    -- rings = compute(fingers) ;
    -- rings = rings * rings + 3;
    -- return rings * var;
    -- ns[5+3] = 10 ;
    -- ns[5+6] = rings ;
    -- rings = ns[5+3] ;
    return;
}
