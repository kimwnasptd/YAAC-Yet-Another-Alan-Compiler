main () : proc
    x : int;
    z : int ;
    -- ns : int[15];
    -- str : byte[100];
    -- b : byte;

    compute (x : reference int) : proc
       -- default val for y is 0
       {
       writeString("\n x from inside compute is: ");
       writeInteger(x);
       writeString("\n Now x from inside compute is: ");
       x = 42;
       writeInteger(x);
       -- y = y + 1;
       -- x = x + 1;    -- NTOE: THIS DOESN'T WORK PROPERLY 
       return ;

       }


{
    -- Main
    x = 1;
    compute(x);
    -- while ( x < 10 ){
    -- compute(x);
    -- x = x + 1 ;
    -- }
    -- compute(x);
    -- compute(x);
    -- compute(x);
    writeString("\n X after the loop is: ");
    writeInteger(x);
    return;
}
