main () : proc
    rings : int;
    ns : int[15];
    str : byte[100];
    -- var : int;
    -- fingers : byte ;
    d : byte;

    compute (x : reference int) : proc
       var : int;
       {
           -- return x[0];
           -- x[7] = 3;
           -- x = readInteger();
           return;
       }

{
    -- Main
    -- compute(rings);
    -- writeInteger(rings);
    -- readString(5, str);
    -- d = readChar();
    writeString("Hello World!\n");
    return;
}
