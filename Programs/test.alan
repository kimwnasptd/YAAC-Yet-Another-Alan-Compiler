main () : proc
    rings : int;
    ns : int[15];
    var : int;

    compute (x : int) : int
        var : int;
        {
            return (x + x ) ;
        }

{
    rings = 2;
    rings = compute(rings);
    return;
}
