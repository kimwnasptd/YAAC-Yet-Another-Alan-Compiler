main () : proc
    rings : int;
    ns : int[15];
    var : int;
    fingers : byte ;

    compute (x : int[]) : int
        var : int;
        {
            return 1;
        }

{
    rings = 2;
    fingers = '5' ;
    rings = - rings ;  
    writeInteger(2);
    -- rings = compute(5);
    -- rings = compute(rings);
    -- rings = compute(fingers) ;
    -- rings = rings * rings + 3;
    -- return rings * var;
    -- ns[5+3] = 10 ;
    -- ns[5+6] = rings ;
    -- rings = ns[5+3] ;
    return;
}
