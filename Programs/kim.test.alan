main () : proc
    a : int;
    frame : byte[10];

    -- nested(b: reference byte[]):proc
    nested():proc
    -- deep():proc
    -- { b[0] = '\x42';}
    {
        a = 17;
        -- frame[0] = '\x42';
        writeByte(frame[0]);
        writeString("\n");
    }

    {
        frame[0] = '\x42';
        nested();
        writeByte(frame[0]);
    }   -- main body
